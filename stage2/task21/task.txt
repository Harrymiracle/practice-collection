思路分析：

  tag 的功能包括：
    增加tag，手动删除tag，超过10个自动删除前面的
    空格、回车、逗号触发增加

  hobby 的功能包括：
    增加hobby，手动删除hobby，超过10个自动删除前面的
    回车，逗号（全角半角均可），顿号，空格（全角半角、Tab）这些符号作为间隔，点击增加按钮触发添加

  抽象出一个类 TagCreater，
    该类有一个queue属性用于存储输入值，

    抽象出针对queue的公共方法，
      尾部添加(可能是一个或是多个)
      头部删除(可能是一个或是多个)
      删除某个(被点中项)

      DOM操作方法 
      
        
  
  监听事件：
    input 监听
    增加button监听

